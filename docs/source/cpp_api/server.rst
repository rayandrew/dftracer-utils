:description: Reference for the async HTTP server: the TCP listener, HTTP parser, router, and connection handling that expose trace APIs over the index.

HTTP Server
===========

The async HTTP server in ``dftracer::utils::server``: the TCP listener, HTTP
parser, router, and connection handling that expose trace query and
visualization APIs over the index.

Type relationships
------------------

How the HTTP server types compose:

.. mermaid:: /_generated/server.mmd

.. include:: /cpp_api/_generated/server.rst.inc
.. include:: /cpp_api/_generated/server.parser.rst.inc
