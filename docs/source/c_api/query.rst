:description: The flat C ABI to the query DSL: build predicates, encode them, and evaluate them over columns via query/abi.h.

Query
=====

.. seealso::

   :doc:`../cpp_api/query` for the C++ query DSL that backs this ABI.

The flat C interface to the query DSL: build predicates, encode them, and
evaluate them over columns. Include ``dftracer/utils/query/abi.h``.

.. doxygenfile:: dftracer/utils/query/abi.h
   :project: dftracer-utils
